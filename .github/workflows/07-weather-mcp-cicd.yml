name: 07 Weather MCP CI CD

on:
  push:
    paths:
      - "07_multi-agent-service-ops/00_runtime-and-deployment/05_weather-mcp-deployment-project/backend/**"
      - "07_multi-agent-service-ops/00_runtime-and-deployment/05_weather-mcp-deployment-project/frontend/**"
      - "07_multi-agent-service-ops/00_runtime-and-deployment/05_weather-mcp-deployment-project/mcp_server/**"
      - "07_multi-agent-service-ops/00_runtime-and-deployment/05_weather-mcp-deployment-project/compose.yml"
  pull_request:
    paths:
      - "07_multi-agent-service-ops/00_runtime-and-deployment/05_weather-mcp-deployment-project/backend/**"
      - "07_multi-agent-service-ops/00_runtime-and-deployment/05_weather-mcp-deployment-project/frontend/**"
      - "07_multi-agent-service-ops/00_runtime-and-deployment/05_weather-mcp-deployment-project/mcp_server/**"
      - "07_multi-agent-service-ops/00_runtime-and-deployment/05_weather-mcp-deployment-project/compose.yml"
  workflow_dispatch:
    inputs:
      deploy:
        description: "Deploy 05 application to AWS EC2"
        required: true
        type: boolean
        default: false

permissions:
  contents: read

concurrency:
  group: weather-mcp-${{ github.ref }}
  cancel-in-progress: false

jobs:
  test-and-build:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: 07_multi-agent-service-ops/00_runtime-and-deployment/05_weather-mcp-deployment-project
    steps:
      - name: Checkout source
        uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - name: Install backend test dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r backend/requirements.txt pytest
      - name: Test backend contract with Fake MCP and LLM
        working-directory: 07_multi-agent-service-ops/00_runtime-and-deployment/05_weather-mcp-deployment-project/backend
        run: python -m pytest test_app.py -q
      - name: Validate Compose
        run: docker compose config --quiet
      - name: Build three images
        run: docker compose build

  deploy:
    if: >-
      (github.event_name == 'push' && github.ref == 'refs/heads/main') ||
      (github.event_name == 'workflow_dispatch' && inputs.deploy)
    needs: test-and-build
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Checkout source
        uses: actions/checkout@v4
      - name: Configure verified SSH identity
        env:
          SSH_PRIVATE_KEY: ${{ secrets.AWS_SSH_PRIVATE_KEY }}
          AWS_SSH_KNOWN_HOSTS: ${{ secrets.AWS_SSH_KNOWN_HOSTS }}
        run: |
          install -m 700 -d ~/.ssh
          printf '%s' "$SSH_PRIVATE_KEY" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          printf '%s\n' "$AWS_SSH_KNOWN_HOSTS" > ~/.ssh/known_hosts
          chmod 600 ~/.ssh/known_hosts
      - name: Copy application source
        env:
          AWS_HOST: ${{ secrets.AWS_HOST }}
          AWS_USER: ${{ secrets.AWS_USER }}
        run: |
          ssh "$AWS_USER@$AWS_HOST" 'mkdir -p ~/weather-mcp-deployment'
          scp -r 07_multi-agent-service-ops/00_runtime-and-deployment/05_weather-mcp-deployment-project/. "$AWS_USER@$AWS_HOST:~/weather-mcp-deployment/"
      - name: Deploy and verify readiness
        env:
          AWS_HOST: ${{ secrets.AWS_HOST }}
          AWS_USER: ${{ secrets.AWS_USER }}
        run: |
          ssh "$AWS_USER@$AWS_HOST" 'cd ~/weather-mcp-deployment && test -f .env && docker compose config --quiet && docker compose up -d --build && curl --fail --retry 12 --retry-delay 5 http://127.0.0.1:8000/health/ready'
